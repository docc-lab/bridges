Legacy pre-PCR scratch files (hybrid-reconstruction era, last touched in
commit 11900ad). Not part of any build: renamed to .txt so the Go
toolchain never parses them (they contain mutually conflicting
declarations and reference the old JSON-direct pipeline superseded by
corpus/ + loader/). Kept for archaeology only.
